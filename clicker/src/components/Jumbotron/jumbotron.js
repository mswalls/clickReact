import React from "react";
import "./jumbotron.css";

const Jumbotron = () => (
	<header className = "header">
		<h1>Overwatch Clicky Game!</h1>
		<h2>Click any Hero to start the game, just don't click the same hero twice!</h2>
	</header>
);

export default Jumbotron;